{
  "name": "claude-code-telegram-notifier",
  "version": "1.0.0",
  "description": "Claude Code CLI plugin that sends Telegram notifications when approval or task completion is needed",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/**/*.ts",
    "prepare": "npm run build"
  },
  "keywords": [
    "claude-code",
    "plugin",
    "telegram",
    "notifications",
    "cli"
  ],
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "@anthropic-ai/claude-code": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.64.0"
  }
}
